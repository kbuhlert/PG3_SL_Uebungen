package UebungImUnterricht.Ue11_CSVexportierenEinlesen;

public class AdressManager {

    //Constructor
    //GetAdress-Methode (return Adress)

    //Methode addAdress

    /*exportFromCSV(String path, String separator) throws AdressExportException
    * neues File erstellen (mit FileWriter drüber gelegt)
    * PrintWriter
    * for-Loop um jede Adresse zu holen (aus getAdress())
    * pro Zeile: printwriter.println hole ersten Namen (getFirstname + Separaator + getLastname...)
    * Printwriter/Bufferedwriter beides möglich --> PW ermöglicht das Auslesen der einzelnen Lines, buffered Writer braucht
    * zusätzlich Befehl bw.newLine();
    * */

    /*loadFromCSV(String path, String separator) throws AdressExportException
     * adress.removalAll(adresses) --> damit die ursprüngliche Liste, die bereits erstellt wurde nicht dupliziert wird
     *holt Adressen, erwartet 4 Spalten
     *neue Adressen werden geholt und in String-Array erstellt
     */

}
